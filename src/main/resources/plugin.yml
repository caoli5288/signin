name: sign
main: com.i5mc.sign.Main
version: ${project.version}

commands:
  sign:
    description: None.
    permission: signin.use

permissions:
  signin.use:
    description: None.
    default: true

depend: [SimpleORM, PlayerPoints, PlaceholderAPI]
